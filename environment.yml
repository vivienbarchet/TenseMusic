name: binder_env
channels:
  - defaults
dependencies:
  - python=3.9
  - pandas
  - librosa
  - numpy
  - matplotlib
  - ipython
  - libsndfile
  - ffmpeg
  - ipykernel
prefix: /opt/anaconda3/envs/binder_env
