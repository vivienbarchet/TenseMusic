name: binder_env
channels:
  - conda-forge
  - defaults
dependencies:
  - pandas
  - pip 
  - numpy
  - matplotlib
  - scipy
  - ffmpeg
  - libsndfile
  - pip:
    - librosa
    - IPython
    - dissonant
    - spleeter
    - mir_eval
    - crepe
    - soundfile
    - pydub
prefix: /opt/anaconda3/envs/binder_env
